# Enterprise Search Example

Domain-specific RAG pipeline for enterprise document search.

## Usage

```bash
python run.py
```

This example demonstrates how to build a multi-domain enterprise search pipeline with domain-aware chunking, hybrid retrieval, and cross-encoder reranking.
